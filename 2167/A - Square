#include <bits/stdc++.h>
using namespace std;
 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    int t;
    cin>>t;
    while(t--){
        int a;
        int b;
        int c;
        int d;
        cin>>a>>b>>c>>d;
 
        if((a==b)&&(b==c)&&(c==d)){
            cout<<"yes"<<'
';
        }
        else{
            cout<<"no"<<'
';
        }
    }
 
    
 
    return 0;
}